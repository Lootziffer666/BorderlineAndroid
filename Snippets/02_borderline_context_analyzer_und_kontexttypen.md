# Borderline2: Context Analyzer und Kontexttypen

- Seiten: 3-5

data class QuickActionContent( override val id: String, override val label: String, val actionType: QuickActionType, val contextTypes: Set<ContextType> = setOf(ContextType.UNKNOWN), val priority: Int = 50, // 0-100, höher = mehr Sichtbarkeit val customAction: String? = null, // For CUSTOM type override val createdAt: Long = System.currentTimeMillis(), override val isFavorite: Boolean = false, val triggeredCount: Int = 0 // Für ML-basierte Vorhersage ) : AccessibilityContent(), Serializable { override fun getThumbnail(): String? = null } 2. CONTEXT ANALYZER (Smart Action Detection) Kotlin // borderline-android/feature-accessibility/src/main/kotlin/ContextAnalyzer.kt interface ContextListener { fun onContextChanged(context: ContextType) } class ContextAnalyzer( private val accessibilityService: AccessibilityService, private val listener: ContextListener ) { private var currentContext = ContextType.UNKNOWN private val observers = mutableListOf<ContextListener>() init { startMonitoring() } private fun startMonitoring() { // Polling-basiert (simplest) oder via Accessibility Events (better) Thread { while (true) { val newContext = analyzeCurrentContext() if (newContext != currentContext) { currentContext = newContext notifyListeners() } Thread.sleep(500) // Check every 500ms } }.start() }

private fun analyzeCurrentContext(): ContextType { return try { val rootNode = accessibilityService.rootInActiveWindow ?: return
ContextType.UNKNOWN
// Prüfe auf Text-Eingabe (IME) if (hasVisibleInputMethod()) { return ContextType.TEXT_EDITING } // Prüfe auf Text-Auswahl if (hasSelectedText(rootNode)) { return ContextType.READING } // Prüfe auf Navigation App val packageName = rootNode.packageName?.toString() ?: "" if (isNavigationApp(packageName)) { return ContextType.NAVIGATION } // Prüfe auf Media/Gallery if (isMediaApp(packageName)) { return ContextType.MEDIA_VIEWING } ContextType.UNKNOWN } catch (e: Exception) { ContextType.UNKNOWN } } private fun hasVisibleInputMethod(): Boolean { // Prüfe ob Tastatur sichtbar ist val inputMethodManager = accessibilityService.getSystemService( Context.INPUT_METHOD_SERVICE ) as? InputMethodManager return inputMethodManager?.isActive ?: false } private fun hasSelectedText(node: AccessibilityNodeInfo): Boolean { return try { node.text?.isNotEmpty() == true || node.contentDescription?.isNotEmpty() == true } catch (e: Exception) { false } }

private fun isNavigationApp(packageName: String): Boolean { return packageName.contains("maps", ignoreCase = true) || packageName.contains("navigation", ignoreCase = true) || packageName.contains("waze", ignoreCase = true) } private fun isMediaApp(packageName: String): Boolean { return packageName.contains("gallery", ignoreCase = true) || packageName.contains("photos", ignoreCase = true) || packageName.contains("camera", ignoreCase = true) || packageName.contains("video", ignoreCase = true) } private fun notifyListeners() { listeners.forEach { it.onContextChanged(currentContext) } } fun addListener(listener: ContextListener) { observers.add(listener) } fun removeListener(listener: ContextListener) { observers.remove(listener) } fun getCurrentContext(): ContextType = currentContext } 3. REPOSITORY MIT SMART RANKING Kotlin // borderline-android/core/src/main/kotlin/QuickActionRepository.kt interface QuickActionRepository { fun getAll(): List<QuickActionContent> fun getSuggestedForContext(context: ContextType): List<QuickActionContent> fun add(action: QuickActionContent) fun update(action: QuickActionContent) fun delete(id: String) fun recordUsage(id: String) } class JsonQuickActionRepository( private val context: Context ) : QuickActionRepository { private val prefs = context.getSharedPreferences("quick_actions",
Context.MODE_PRIVATE)
private val gson = Gson()
