# Borderline2: Setup Wizard und Onboarding

- Seiten: 8-11

private fun showSetupDialog(activity: Activity, onComplete: () -> Unit) { val steps = listOf( SetupStep.Welcome, SetupStep.Permissions, SetupStep.QuickStart, SetupStep.Customize ) var currentStep = 0 fun showStep(step: SetupStep) { val view = buildStepView(activity, step) AlertDialog.Builder(activity) .setView(view) .setNegativeButton(if (currentStep == 0) "Skip" else "Back") { _, _ -> if (currentStep > 0) { currentStep-- showStep(steps[currentStep]) } else { markSetupComplete() onComplete() } } .setPositiveButton(if (currentStep == steps.size - 1) "Finish" else "Next") { _, _ -> if (currentStep < steps.size - 1) { currentStep++ showStep(steps[currentStep]) } else { markSetupComplete() onComplete() } } .setCancelable(false) .show() } showStep(steps[0]) } private fun buildStepView(activity: Activity, step: SetupStep): View { return when (step) { SetupStep.Welcome -> buildWelcomeView(activity) SetupStep.Permissions -> buildPermissionsView(activity) SetupStep.QuickStart -> buildQuickStartView(activity) SetupStep.Customize -> buildCustomizeView(activity) }

} private fun buildWelcomeView(activity: Activity): View { return LinearLayout(activity).apply { orientation = LinearLayout.VERTICAL layoutParams = LinearLayout.LayoutParams( LinearLayout.LayoutParams.MATCH_PARENT, LinearLayout.LayoutParams.WRAP_CONTENT ) addView(TextView(activity).apply { text = "Welcome to Borderline" textSize = 24f setTypeface(null, android.graphics.Typeface.BOLD) layoutParams = LinearLayout.LayoutParams( LinearLayout.LayoutParams.MATCH_PARENT, LinearLayout.LayoutParams.WRAP_CONTENT ).apply { setMargins(16.dp, 16.dp, 16.dp, 8.dp) } }) addView(TextView(activity).apply { text = "Accessibility-Overlay für schnellere Textbearbeitung, Clipboard-Verwaltung
und

Smart

Actions."
textSize = 14f layoutParams = LinearLayout.LayoutParams( LinearLayout.LayoutParams.MATCH_PARENT, LinearLayout.LayoutParams.WRAP_CONTENT ).apply { setMargins(16.dp, 8.dp, 16.dp, 16.dp) } }) } } private fun buildPermissionsView(activity: Activity): View { return LinearLayout(activity).apply { orientation = LinearLayout.VERTICAL addView(TextView(activity).apply { text = "Erforderliche Berechtigungen" textSize = 18f setTypeface(null, android.graphics.Typeface.BOLD) }) addView(CheckBox(activity).apply { text = " ✓ Accessibility Service" isChecked = true isEnabled = false })

addView(CheckBox(activity).apply { text = " ✓ Clipboard-Zugriff" isChecked = true isEnabled = false }) addView(CheckBox(activity).apply { text = " ✓ Overlay-Fenster" isChecked = true isEnabled = false }) addView(TextView(activity).apply { text = "Diese werden bei Aktivierung automatisch eingefordert." textSize = 12f setTextColor(Color.GRAY) layoutParams = LinearLayout.LayoutParams( LinearLayout.LayoutParams.MATCH_PARENT, LinearLayout.LayoutParams.WRAP_CONTENT ).apply { setMargins(0, 16.dp, 0, 0) } }) } } private fun buildQuickStartView(activity: Activity): View { return LinearLayout(activity).apply { orientation = LinearLayout.VERTICAL addView(TextView(activity).apply { text = "Quick Start" textSize = 18f setTypeface(null, android.graphics.Typeface.BOLD) }) addView(TextView(activity).apply { text = "Wische von der Seite um die Menüs zu öffnen:\n\n" + "← Links: Snippets & Clipboard\n" + "→ Rechts: Shortcuts & Quick Actions\n\n" + "Halte länger um zu bearbeiten." textSize = 14f layoutParams = LinearLayout.LayoutParams( LinearLayout.LayoutParams.MATCH_PARENT, LinearLayout.LayoutParams.WRAP_CONTENT ).apply { setMargins(0, 16.dp, 0, 0) } }) } }

private fun buildCustomizeView(activity: Activity): View { return LinearLayout(activity).apply { orientation = LinearLayout.VERTICAL addView(TextView(activity).apply { text = "Anpassen" textSize = 18f setTypeface(null, android.graphics.Typeface.BOLD) }) addView(CheckBox(activity).apply { text = "Adaptive Quick Actions aktivieren" isChecked = true }) addView(CheckBox(activity).apply { text = "Clipboard-Auto-Grab" isChecked = true }) addView(CheckBox(activity).apply { text = "Haptic Feedback" isChecked = true }) } } } sealed class SetupStep { object Welcome : SetupStep() object Permissions : SetupStep() object QuickStart : SetupStep() object Customize : SetupStep() } 5. PANEL MIT THUMBNAILS, EDIT MODE UND + BUTTON Kotlin // borderline-android/feature-overlay/src/main/kotlin/panels/SmartPanel.kt abstract class SmartPanel( protected val context: Context, protected val panelId: Int, protected val edge: ScreenEdge ) { var isEditMode = false var isVisible = false protected lateinit var rootView: FrameLayout
